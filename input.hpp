#ifndef __INPUT__H__
#define __INPUT__H__

struct key<int code> {
  using a = key<SDLK_a>;
  using b = key<SDLK_b>;
  using c = key<SDLK_c>;
  using d = key<SDLK_d>;
  using e = key<SDLK_e>;
  using f = key<SDLK_f>;
  using g = key<SDLK_g>;
  using h = key<SDLK_h>;
  using i = key<SDLK_i>;
  using j = key<SDLK_j>;
  using k = key<SDLK_k>;
  using l = key<SDLK_l>;
  using m = key<SDLK_m>;
  using n = key<SDLK_n>;
  using o = key<SDLK_o>;
  using p = key<SDLK_p>;
  using q = key<SDLK_q>;
  using r = key<SDLK_r>;
  using s = key<SDLK_s>;
  using t = key<SDLK_t>;
  using u = key<SDLK_u>;
  using v = key<SDLK_v>;
  using w = key<SDLK_w>;
  using x = key<SDLK_x>;
  using y = key<SDLK_y>;
  using z = key<SDLK_z>;
  using n0 = key<SDLK_0>;
  using n1 = key<SDLK_1>;
};

#endif